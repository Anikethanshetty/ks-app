# React Native / Metro does not support pnpm's default isolated node_modules
# (transitive deps like @expo/metro-runtime aren't resolvable). Hoist to a flat
# layout so Metro can resolve everything.
node-linker=hoisted
