import { Placeholder } from "@/components/Placeholder";
import { SignOutButton } from "@/components/SignOutButton";

export default function DeliveryHome() {
  return (
    <Placeholder
      title="ಡೆಲಿವರಿ"
      subtitle="Deliveries + amount to collect (Phase 4)"
      footer={<SignOutButton />}
    />
  );
}
