import { Placeholder } from "@/components/Placeholder";
import { SignOutButton } from "@/components/SignOutButton";

export default function CustomerHome() {
  return (
    <Placeholder
      title="ಗ್ರಾಹಕ"
      subtitle="Customer home — the mic stage (Phase 5)"
      footer={<SignOutButton />}
    />
  );
}
